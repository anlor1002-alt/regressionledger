## What this changes

<!-- One or two sentences. Link any related issue. -->

## Checklist

- [ ] `npm test` passes (and I added a test for any behavior change)
- [ ] Stays **dependency-free** (no new runtime dependencies)
- [ ] Hooks still **fail open** (no path can throw out of `rl hook`)
- [ ] Hook mode keeps **stdout clean** (only JSON; logs go to stderr behind `RL_DEBUG`)
- [ ] Updated `README` / `CHANGELOG` if user-facing

## Notes for reviewers

<!-- Anything tricky, trade-offs, or follow-ups. -->
